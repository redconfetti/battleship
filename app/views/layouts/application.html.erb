<!DOCTYPE html>
<html lang="en" data-ng-app="battleship" data-ng-controller="BattleshipController">
<head>
  <title>Battleship</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => false %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => false %>
  <script src="https://js.pusher.com/3.0/pusher.min.js"></script>
  <%= csrf_meta_tags %>
</head>
<body>

	<nav class="navbar navbar-inverse navbar-fixed-top">
	  <div class="container">
			<div class="navbar-header">
			  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
			    <span class="sr-only">Toggle navigation</span>
			    <span class="icon-bar"></span>
			    <span class="icon-bar"></span>
			    <span class="icon-bar"></span>
			  </button>
			  <a class="navbar-brand" href="/#/">Battleship</a>
			</div>
      <div data-ng-if="loginError">
        <p class="alert alert-danger">
          <span data-ng-bind="loginError"></span>
        </p>
      </div>

      <div id="navbar" class="navbar-collapse collapse">

        <form class="navbar-form navbar-right" ng-controller="LoginController">
          <button class="btn btn-primary" data-ng-click="goToLogin()" data-ng-if="!isAuthenticated">Login</button>
          <button class="btn btn-primary" data-ng-click="destroySession()" data-ng-if="isAuthenticated">Logout</button>          
        </form>
	    </div><!--/.navbar-collapse -->
    </div>
  </nav>

  <div class="container">
    <%= yield %>
  </div>

</body>
</html>
