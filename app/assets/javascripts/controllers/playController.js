angular.module('battleship').controller('PlayController', ['$http', '$routeParams', '$scope', function PlayController($http, $routeParams, $scope) {
  // var gameId = $routeParams.gameId;
  // console.log(gameId);
}]);
